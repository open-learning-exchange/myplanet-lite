<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <FrameLayout
            android:id="@+id/teamContentContainer"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="gone" />

        <TextView
            android:id="@+id/teamsEmptyView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:paddingStart="@dimen/dashboard_horizontal_padding"
            android:paddingEnd="@dimen/dashboard_horizontal_padding"
            android:textColor="?attr/colorOnSurface"
            android:textSize="@dimen/text_size_16sp" />
    </FrameLayout>

    <LinearLayout
        android:id="@+id/teamActionsBar"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:gravity="center"
        android:orientation="horizontal"
        android:paddingStart="@dimen/dashboard_horizontal_padding"
        android:paddingEnd="@dimen/dashboard_horizontal_padding"
        android:paddingTop="8dp"
        android:paddingBottom="8dp">

        <ImageButton
            android:id="@+id/teamVoicesButton"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/dashboard_voices_title"
            android:padding="8dp"
            android:src="@drawable/ic_comment_24"
            app:tint="?attr/colorOnSurface" />

        <Space
            android:layout_width="16dp"
            android:layout_height="match_parent" />

        <ImageButton
            android:id="@+id/teamSurveysButton"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/dashboard_surveys_title"
            android:padding="8dp"
            android:src="@drawable/ic_surveys_24"
            app:tint="?attr/colorOnSurface" />
    </LinearLayout>

</LinearLayout>
