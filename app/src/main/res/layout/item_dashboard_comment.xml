<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="12dp"
    android:paddingBottom="12dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/commentAvatar"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="@drawable/avatar_placeholder_background"
            android:clipToOutline="true"
            android:contentDescription="@string/dashboard_avatar_content_description"
            android:scaleType="centerCrop" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/commentAuthor"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="?attr/colorOnSurface"
                android:textStyle="bold"
                android:textSize="@dimen/text_size_13sp"
                tools:text="Walfre Lopez" />

            <TextView
                android:id="@+id/commentMetadata"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:alpha="0.72"
                android:textColor="?attr/colorOnSurface"
                android:textSize="@dimen/text_size_11sp"
                tools:text="@wlopez • 1h" />

        </LinearLayout>

        <ImageButton
            android:id="@+id/commentOverflowMenu"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/dashboard_comment_actions_menu_content_description"
            android:padding="8dp"
            android:src="@drawable/ic_more_vert_24"
            app:tint="?attr/colorOnSurface" />

    </LinearLayout>

    <TextView
        android:id="@+id/commentBody"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textColor="?attr/colorOnSurface"
        android:textSize="@dimen/text_size_13sp"
        tools:text="Gracias por compartir esta información." />

    <LinearLayout
        android:id="@+id/commentImagesContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:visibility="gone"
        tools:visibility="visible">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:background="@drawable/dashboard_post_image_placeholder"
            android:contentDescription="@string/dashboard_post_image_content_description"
            android:scaleType="fitCenter"
            tools:src="@color/dashboard_post_divider" />

    </LinearLayout>

    <View
        android:id="@+id/commentDivider"
        android:layout_width="match_parent"
        android:layout_height="@dimen/dashboard_post_divider_height"
        android:layout_marginTop="12dp"
        android:background="@color/dashboard_post_divider"
        tools:visibility="visible" />

</LinearLayout>
