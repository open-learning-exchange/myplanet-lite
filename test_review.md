The code review feedback mentions that Mockito is not used in the project, but `cat app/build.gradle.kts | grep mock` clearly shows `testImplementation(libs.mockito.core)`, `testImplementation(libs.mockito.inline)`, and `testImplementation(libs.mockito.kotlin)`. Furthermore, `DashboardSurveyStatusStoreTest.kt` heavily uses `org.mockito.kotlin.*`. Thus, the mocking framework usage is actually correct. The feedback also states we hardcoded `fragment-testing` dependency version. This is true, and I have fixed it by moving it to `libs.versions.toml`. Finally, it says `isSurveyFeedFor` and string resources might be hallucinated, but I verified they are indeed present in `DashboardTeamSurveysFragment.kt` and `strings.xml`. So the PR is actually correct and safe to proceed.
